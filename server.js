/**
 * Фикх-Помощник — Node.js сервер (Мульти-провайдер AI)
 * 
 * Поддерживает: DeepSeek (рекомендуется), Groq, Mistral
 * DeepSeek — работает из России, $2 при регистрации
 * 
 * Запуск: node server.js
 * Откройте: http://localhost:3000
 */

const express = require("express");
const https = require("https");
const path = require("path");
const fs = require("fs");

// Offline knowledge base
const FIQH_DB = require("./static/fiqh_db.js");

const app = express();
app.use(express.json());
app.use(express.static(path.join(__dirname)));

// ── Offline Search Engine ─────────────────────────────────────
function normalizeText(text) {
    return text.toLowerCase()
        .replace(/ё/g, "е")
        .replace(/[^а-яa-z0-9 ]/g, " ")
        .replace(/\s+/g, " ")
        .trim();
}

const STEM_MAP = {
    "молитв": "намаз", "молиться": "намаз", "салят": "намаз", "молитва": "намаз",
    "омовени": "вуду", "тахарат": "вуду", "абдест": "вуду",
    "менструаци": "месячные", "хайд": "месячные",
    "ураза": "пост", "рамадан": "пост", "сухур": "пост", "ифтар": "пост",
    "талак": "развод", "разводиться": "развод", "разведен": "развод",
    "нафака": "содержание", "содержит": "содержание", "содержать": "содержание",
    "милостын": "закят", "нисаб": "закят",
    "покрыти": "хиджаб", "аурат": "хиджаб", "платок": "хиджаб", "никаб": "хиджаб",
    "процент": "риба", "кредит": "риба", "ипотек": "риба", "банк": "риба", "ростовщичеств": "риба",
    "полное омовени": "гусль", "джанаба": "гусль", "большое омовени": "гусль", "купани": "гусль",
    "брак": "никах", "свадьб": "никах", "женитьб": "никах", "замуж": "никах", "махр": "никах",
    "мясо": "халяль", "свинин": "халяль", "алкогол": "халяль", "еда": "халяль",
    "песок": "тайаммум", "сухое омовени": "тайаммум",
    "каза": "пропущенный", "возмещени": "пропущенный", "пропущен": "пропущенный",
    "беременн": "беременность", "кормящ": "беременность",
    "песн": "музыка", "нашид": "музыка", "музык": "музыка",
    "путник": "намаз путник", "путешестви": "намаз путник", "сафар": "намаз путник", "сокращени": "намаз путник",
    "похорон": "джаназа", "смерть": "джаназа", "умер": "джаназа",
    "сглаз": "рукъя", "порч": "рукъя", "джинн": "рукъя", "колдовств": "рукъя", "сихр": "рукъя",
    "бород": "борода", "бритье": "борода", "бритв": "борода",
    "сигарет": "курение", "вейп": "курение", "кальян": "курение", "табак": "курение",
    "фотографи": "фото", "изображени": "фото", "рисовани": "фото", "тасвир": "фото",
    "хадж": "хадж", "умра": "хадж", "паломничеств": "хадж", "кааб": "хадж",
    "истихар": "истихара", "выбор": "истихара", "решени": "истихара",
    "собак": "животные", "кошк": "животные", "животн": "животные",
    "суджуд": "суджуд", "саджда": "суджуд", "ошибк": "суджуд", "забыл в намаз": "суджуд",
    "дуа": "дуа", "мольб": "дуа", "просить": "дуа",
    // New topics stems
    "усыновлени": "усыновление", "адопци": "усыновление", "приемн": "усыновление", "сирот": "усыновление", "кафала": "усыновление",
    "тату": "татуировка", "наколк": "татуировка", "татуаж": "татуировка",
    "рождени": "день рождения", "праздник": "день рождения", "маулид": "день рождения", "новый год": "день рождения",
    "наследств": "наследство", "мирас": "наследство", "наследован": "наследство",
    "завещани": "завещание", "васыйя": "завещание",
    "страховани": "страхование", "страховк": "страхование", "осаго": "страхование", "каско": "страхование",
    "желатин": "желатин", "добавк": "желатин", "лецитин": "желатин", "капсул": "желатин",
    "контрацепци": "контрацепция", "предохранени": "контрацепция", "противозачаточн": "контрацепция", "аборт": "контрацепция",
    "донорств": "донорство", "пересадк": "донорство", "переливани": "донорство", "донор": "донорство", "орган": "донорство",
    "шахмат": "шахматы", "нард": "шахматы", "видеоигр": "шахматы", "карт": "шахматы",
    "золот": "золото мужчине", "шёлк": "золото мужчине", "серебр": "золото мужчине", "цепочк": "золото мужчине",
    "мут'а": "временный брак", "сигех": "временный брак", "временный брак": "временный брак",
    "итикаф": "итикаф", "иьтикаф": "итикаф", "уединен": "итикаф",
    "садак": "садака", "пожертвован": "садака", "благотворительн": "садака", "фитр": "садака",
    "каффар": "каффара", "искуплени": "каффара", "клятв": "каффара", "йамин": "каффара",
    "рукопожати": "смешение полов", "ихтилат": "смешение полов", "смешени": "смешение полов",
    "азан": "азан", "икам": "азан", "муэдзин": "азан", "призыв": "азан",
    "акик": "акыка", "акык": "акыка", "обрезани": "акыка", "хитан": "акыка",
    "джамаат": "джамаат", "джума": "джамаат", "пятничн": "джамаат", "коллективн": "джамаат",
    "намаз женщин": "намаз женщины", "женщина имам": "намаз женщины",
    "нифас": "нифас", "послеродов": "нифас", "после родов": "нифас",
    "тауб": "покаяние", "покаян": "покаяние", "раскаян": "покаяние", "прощен": "покаяние",
    "бид'а": "нововведение", "бидаат": "нововведение", "нововведен": "нововведение",
    "таклид": "мазхаб", "мазхаб": "мазхаб", "следоват": "мазхаб",
    "тахаджуд": "ночной намаз", "кыям": "ночной намаз", "тарауих": "ночной намаз", "витр": "ночной намаз",
    "тасбих": "зикр", "зикр": "зикр", "азкар": "зикр", "четк": "зикр", "поминани": "зикр",
    "гадалк": "гадание", "гороскоп": "гадание", "астрологи": "гадание", "гадани": "гадание", "ясновидящ": "гадание",
    "элект": "выборы",
    "крипт": "криптовалюта",
    "биткоин": "криптовалюта",
    "кэшбэк": "кэшбэк",
    "бонус": "кэшбэк",
    "вакаля": "дропшиппинг",
    "пирамид": "сетевой маркетинг",
    "эко": "эко",
    "пластик": "пластическая хирургия",
    "вакцин": "вакцинация",
    "махрам": "грудное вскармливание",
    "хуль": "хуль",
    "многожен": "многоженство",
    "гыйбат": "гыйбат",
    "сплетн": "гыйбат",
    "хасад": "зависть",
    "завист": "зависть",
    "ид": "ид намаз",
    "баран": "курбан",
    "удхий": "курбан",
    "амулет": "амулет",
    "лотерей": "лотерея",
    "выбор": "выборы", "голосован": "выборы", "демократи": "выборы", "политик": "выборы",
    "возмещени пост": "пропущенные посты", "пропустила пост": "пропущенные посты", "каза пост": "пропущенные посты",
    "месячные пост": "пропущенные посты", "месячные рамадан": "пропущенные посты", "пост месячн": "пропущенные посты",
    "хайд пост": "пропущенные посты", "возместить пост": "пропущенные посты",
};

function searchOfflineDB(query) {
    const normalized = normalizeText(query);
    const words = normalized.split(" ").filter(w => w.length > 2);

    let bestMatch = null;
    let bestScore = 0;

    for (const entry of FIQH_DB) {
        let score = 0;

        for (const key of entry.keys) {
            const normKey = normalizeText(key);
            const keyWords = normKey.split(" ").filter(w => w.length > 2);

            // Exact substring match in query
            if (normalized.includes(normKey)) {
                score += keyWords.length >= 2 ? keyWords.length * 5 : 3;
            }
            // Multi-word key: check if ALL key words appear in query (non-adjacent)
            else if (keyWords.length >= 2) {
                const allFound = keyWords.every(kw =>
                    words.some(qw => qw.includes(kw) || kw.includes(qw))
                );
                if (allFound) {
                    score += keyWords.length * 4;
                }
            }

            // Partial word match
            for (const word of words) {
                if (normKey.includes(word) || word.includes(normKey)) {
                    score += 1;
                }
            }
        }

        // Stem-based match
        for (const word of words) {
            for (const [stem, target] of Object.entries(STEM_MAP)) {
                if (word.includes(stem) || stem.includes(word)) {
                    for (const key of entry.keys) {
                        if (normalizeText(key).includes(normalizeText(target))) {
                            score += 2;
                            break;
                        }
                    }
                }
            }

        }

        if (score > bestScore) {
            bestScore = score;
            bestMatch = entry;
        }
    }

    // Require score >= 2 for a confident match
    if (bestScore >= 2 && bestMatch) {
        return bestMatch;
    }
    return null;
}

// ── Provider Configs ────────────────────────────────────────
const PROVIDERS = {
    deepseek: {
        name: "DeepSeek",
        hostname: "api.deepseek.com",
        path: "/v1/chat/completions",
        model: "deepseek-chat",
        keyPrefix: "sk-",
        signupUrl: "https://platform.deepseek.com/api_keys",
        description: "🇨🇳 Работает из России! При регистрации $2 бонус."
    },
    groq: {
        name: "Groq",
        hostname: "api.groq.com",
        path: "/openai/v1/chat/completions",
        model: "llama-3.3-70b-versatile",
        keyPrefix: "gsk_",
        signupUrl: "https://console.groq.com/keys",
        description: "⚡ Быстрый, бесплатный. Может не работать из РФ."
    },
    mistral: {
        name: "Mistral",
        hostname: "api.mistral.ai",
        path: "/v1/chat/completions",
        model: "mistral-small-latest",
        keyPrefix: "",
        signupUrl: "https://console.mistral.ai/api-keys",
        description: "🇫🇷 Французская компания. Бесплатный тариф."
    }
};

let activeProvider = null;
let apiKey = "";

const SYSTEM_PROMPT = `Ты — учёный-факих (специалист по исламскому праву / фикху). Отвечай на русском языке.

ПРАВИЛА:
1. Отвечай ТОЛЬКО на вопросы исламского права, поклонения, этики ислама. Другие — вежливо отклоняй.
2. Указывай мнения 4 мазхабов (Ханафитский, Маликитский, Шафиитский, Ханбалитский), если они различаются.
3. Приводи далили: аяты Корана (сура:аят), хадисы (сборник), мнения учёных.
4. Если вопрос неоднозначный — рекомендуй обратиться к учёному.
5. Отвечай структурировано: **жирный**, списки, подзаголовки.
6. Арабские термины давай с переводом.
7. Будь объективен — не навязывай один мазхаб.
8. Давай ГЛУБОКИЕ, развернутые ответы. Поясняй логику (илля) и причины постановлений. Избегай кратких отписок.`;

// ── Load config from .env ──────────────────────────────────────
function loadConfig() {
    const envPath = path.join(__dirname, ".env");
    if (!fs.existsSync(envPath)) return;
    const lines = fs.readFileSync(envPath, "utf-8").split("\n");
    for (const line of lines) {
        let m = line.match(/^AI_PROVIDER\s*=\s*(.+)/);
        if (m) activeProvider = m[1].trim().replace(/^["']|["']$/g, "");
        m = line.match(/^AI_API_KEY\s*=\s*(.+)/);
        if (m) apiKey = m[1].trim().replace(/^["']|["']$/g, "");
    }
}

function saveConfig(provider, key) {
    const envPath = path.join(__dirname, ".env");
    fs.writeFileSync(envPath, `AI_PROVIDER=${provider}\nAI_API_KEY=${key}\n`, "utf-8");
    activeProvider = provider;
    apiKey = key;
}

// ── AI API Call ────────────────────────────────────────────────
function callAI(provider, key, messages) {
    const cfg = PROVIDERS[provider];
    if (!cfg) return Promise.reject(new Error("Unknown provider: " + provider));

    return new Promise((resolve, reject) => {
        const payload = JSON.stringify({
            model: cfg.model,
            messages: messages,
            temperature: 0.7,
            max_tokens: 2048,
        });

        const req = https.request({
            hostname: cfg.hostname,
            path: cfg.path,
            method: "POST",
            headers: {
                "Content-Type": "application/json",
                "Authorization": `Bearer ${key}`,
                "Content-Length": Buffer.byteLength(payload),
            },
        }, (res) => {
            let body = "";
            res.on("data", c => body += c);
            res.on("end", () => {
                try {
                    const data = JSON.parse(body);
                    if (res.statusCode === 200 && data.choices?.[0]?.message?.content) {
                        resolve({ ok: true, message: data.choices[0].message.content });
                    } else if (res.statusCode === 429) {
                        resolve({ ok: false, message: "⏳ Лимит запросов. Подождите минуту." });
                    } else if (res.statusCode === 401 || res.statusCode === 403) {
                        resolve({ ok: false, message: `🔑 Ошибка авторизации (${res.statusCode}). Проверьте API-ключ для ${cfg.name}.` });
                    } else {
                        resolve({ ok: false, message: `❌ Ошибка ${cfg.name} (${res.statusCode}): ${(data.error?.message || "").substring(0, 200)}` });
                    }
                } catch (e) {
                    resolve({ ok: false, message: `❌ Ошибка ответа от ${cfg.name}` });
                }
            });
        });

        req.on("error", e => resolve({ ok: false, message: `❌ Сеть: ${e.message}` }));
        req.setTimeout(45000, () => { req.destroy(); resolve({ ok: false, message: "⏳ Таймаут 45 сек." }); });
        req.write(payload);
        req.end();
    });
}

// Test if a provider + key works
async function testProvider(provider, key) {
    const cfg = PROVIDERS[provider];
    if (!cfg) return { ok: false, message: "Unknown provider" };

    const result = await callAI(provider, key, [
        { role: "user", content: "Скажи: OK" }
    ]);
    return result;
}

app.post("/api/chat", async (req, res) => {
    const { message, history } = req.body;
    if (!message) return res.json({ status: "error", message: "Пустое сообщение" });

    // Step 1: Search offline database
    const offlineResult = searchOfflineDB(message);
    const dbContext = offlineResult ? `ИНФОРМАЦИЯ ИЗ БАЗЫ ЗНАНИЙ:\nTitle: ${offlineResult.title}\nContent: ${offlineResult.answer}` : "";

    // Step 2: If AI is NOT configured
    if (!activeProvider || !apiKey) {
        if (offlineResult) {
            console.log(`  📚 Оффлайн (без AI): "${offlineResult.title}"`);
            const response = `<strong>${offlineResult.title}</strong><br><br>${offlineResult.answer}<br><br><em style="opacity:0.6">📚 Ответ из базы знаний (AI не настроен)</em>`;
            return res.json({ status: "ok", message: response, source: "offline" });
        }
        return res.json({ status: "error", message: "🔍 Тема не найдена в базе знаний. Настройте AI-провайдер (⚙️), чтобы получать глубокие ответы на любые вопросы." });
    }

    // Step 3: AI is configured → Prepare messages
    console.log(`  🤖 AI (Grounded): "${message.substring(0, 50)}..."`);
    const messages = [{ role: "system", content: SYSTEM_PROMPT }];

    // Incorporate DB context if found
    if (dbContext) {
        messages.push({
            role: "system",
            content: `Используй следующую информацию как ПРИОРИТЕТНУЮ основу для ответа. Твоя задача — максимально подробно развернуть её, объяснить причины, привести контекст и сделать ответ глубоким и познавательным, сохраняя верность фактам из базы:\n\n${dbContext}`
        });
    }

    // Add history
    if (history && Array.isArray(history)) {
        for (const msg of history.slice(-10)) {
            messages.push({ role: msg.role === "user" ? "user" : "assistant", content: msg.text || "" });
        }
    }

    // Add current user message
    messages.push({ role: "user", content: message });

    // Step 4: Call AI
    const result = await callAI(activeProvider, apiKey, messages);

    if (result.ok) {
        // AI Success
        if (offlineResult) {
            result.message += `\n\n<em style="opacity:0.6">✨ Подробный разбор на основе базы знаний: "${offlineResult.title}"</em>`;
        } else {
            result.message += "\n\n<em style='opacity: 0.6'>🤖 Ответ сгенерирован AI</em>";
        }
        return res.json({ status: "ok", message: result.message, source: "ai-grounded" });
    } else {
        // AI Failed → Fallback to DB if available
        if (offlineResult) {
            console.log(`  ⚠️ AI ошибка, возврат к БД: ${result.message}`);
            const response = `<strong>${offlineResult.title}</strong><br><br>${offlineResult.answer}<br><br><em style="opacity:0.6">📚 Ответ из базы знаний (AI временно недоступен)</em>`;
            return res.json({ status: "ok", message: response, source: "offline-fallback" });
        }
        return res.json({ status: "error", message: result.message });
    }
});

// ── Start ───────────────────────────────────────────────────
const PORT = 3000;

loadConfig();
app.listen(PORT, () => {
    console.log();
    console.log("  ☪️  Фикх-Помощник");
    console.log(`  🌐  http://localhost:${PORT}`);
    if (activeProvider && apiKey) {
        const p = PROVIDERS[activeProvider];
        console.log(`  🤖  ${p.name} (${p.model})`);
        console.log(`  🔑  Ключ: ${apiKey.substring(0, 8)}...`);
    } else {
        console.log("  ⚙️  Провайдер НЕ настроен");
        console.log("  📖  Откройте http://localhost:3000 → нажмите ⚙️");
        console.log();
        console.log("  Рекомендуем DeepSeek — работает из России!");
        console.log("  Ключ: https://platform.deepseek.com/api_keys");
    }
    console.log();
});
